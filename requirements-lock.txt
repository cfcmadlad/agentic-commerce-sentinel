# Full dependency closure, generated via `pip freeze` from the environment
# these Day 1 modules were built and tested against. pyproject.toml pins
# direct dependencies; this file additionally pins every transitive
# dependency, so `pip install -r requirements-lock.txt` reproduces the
# exact environment rather than "whatever resolves today" against the
# pyproject.toml ranges.
#
# Regenerate after any dependency change with:
#   pip install -e ".[dev]" && pip freeze --exclude-editable > requirements-lock.txt

annotated-types==0.8.0
ast_serialize==0.8.0
cffi==2.1.1
cryptography==50.0.0
iniconfig==2.3.0
joblib==1.5.3
librt==0.15.0
mypy==2.3.1
mypy_extensions==1.1.0
narwhals==2.25.0
numpy==2.5.2
packaging==26.3
pathspec==1.1.1
pluggy==1.6.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
pytest==9.1.1
ruff==0.16.4
scikit-learn==1.9.0
scipy==1.18.1
threadpoolctl==3.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0