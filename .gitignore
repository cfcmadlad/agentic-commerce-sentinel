__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg
build/
dist/

# Generated synthetic data and eval artifacts are large and reproducible
# from the committed generator; do not commit them.
/data/
/artifacts/
*.parquet

# API service's runtime audit log -- append-only, grows with every decision
# a running service instance makes; not something to check in.
service_audit.jsonl

.DS_Store
.env

# Local tooling config and planning docs — not part of the shipped project.
/.claude/
/docs/PROJECT_PLAN.md