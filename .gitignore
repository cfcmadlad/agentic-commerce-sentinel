__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg
build/
dist/

# Generated synthetic data and eval artifacts are large and reproducible
# from the committed generator; do not commit them.
/data/
/artifacts/
*.parquet

# API service's runtime audit log -- append-only, grows with every decision
# a running service instance makes; not something to check in.
service_audit.jsonl

# Same reasoning, for the escalation queue's own hash-chained log.
service_escalations.jsonl

# Same reasoning, for run_full_eval.py's own hash-chained manifest log.
eval_manifests.jsonl

.DS_Store
.env

# Local tooling config and planning docs — not part of the shipped project.
/.claude/
/docs/PROJECT_PLAN.md
/docs/EXECUTION_BRIEF_AGENT_SPRINT.md
.vercel
